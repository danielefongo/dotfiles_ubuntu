- defaults:
    link:
      create: true
      relink: true
      force: true
    shell:
      stdout: true
      stderr: true

- link:
    ~/.gitconfig: dots/.gitconfig
    ~/.gitignore_global: dots/.gitignore_global
    ~/.zshrc: dots/.zshrc

- shell:
    - [bash install.sh]